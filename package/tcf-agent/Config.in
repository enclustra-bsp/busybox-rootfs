config BR2_PACKAGE_TCF_AGENT
	bool "tcf-agent"
	help
    Target Communication Framework is a lightweight, extensible network
    protocol for driving embedded systems (targets).
    It is required to debug Xilinx SDSoC applications.
